version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: BackEnd/Dockerfile
    container_name: taskmind-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/taskmind.db
      - GROQ_API_KEY=${GROQ_API_KEY}
    env_file:
      - .env
    networks:
      - taskmind-network
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: FrontEnd/Dockerfile
    container_name: taskmind-frontend
    ports:
      - "8501:8501"
    networks:
      - taskmind-network
    depends_on:
      - backend
    environment:
      - BACKEND_URL=http://backend:8000
    restart: unless-stopped

  # Ollama removed - using Groq API instead

networks:
  taskmind-network:
    driver: bridge